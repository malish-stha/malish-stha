name: Metrics

on:
  # Schedule daily updates
  schedule:
    - cron: "0 0 * * *"
  # Optional: Run workflow manually
  workflow_dispatch:
  # Optional: Run workflow when pushing on master/main
  push:
    branches:
      - master
      - main

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    environment: 
      name: production
    permissions:
      contents: write

    steps:
      - name: Generate Full-year Calendar Metrics
        id: generate-full-year-calendar-metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          output_action: 'commit'
          filename: metrics.plugin.isocalendar.fullyear.svg
          base: ""
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

      - name: Generate Full History Calendar Metrics
        id: generate-full-history-calendar-metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          output_action: 'commit'
          filename: metrics.plugin.calendar.full.svg
          base: ""
          plugin_calendar: yes
          plugin_calendar_limit: 0

      - name: Generate Succinct Report with PWA Metrics
        id: generate-succinct-report-with-pwa-metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.pagespeed.svg
          token: NOT_NEEDED
          base: ""
          plugin_pagespeed: yes
          plugin_pagespeed_token: ${{ secrets.PAGESPEED_TOKEN }}
          plugin_pagespeed_url: https://lecoq.io
          plugin_pagespeed_pwa: yes

      - name: Generate Featured Repositories Metrics
        id: generate-featured-repositories-metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.repositories.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_repositories: yes
          plugin_repositories_featured: lowlighter/metrics

      - name: Generate Pinned Repositories Metrics
        id: generate-pinned-repositories-metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.repositories.pinned.svg
          token: ${{ secrets.METRICS_TOKEN }}
          base: ""
          plugin_repositories: yes
          plugin_repositories_pinned: 2

      - name: Commit Metrics
        if: |
          steps.generate-full-year-calendar-metrics.outputs.output_action == 'commit' ||
          steps.generate-full-history-calendar-metrics.outputs.output_action == 'commit' ||
          steps.generate-succinct-report-with-pwa-metrics.outputs.output_action == 'commit' ||
          steps.generate-featured-repositories-metrics.outputs.output_action == 'commit' ||
          steps.generate-pinned-repositories-metrics.outputs.output_action == 'commit'
        uses: EndBug/add-and-commit@v9
        with:
          add: 'metrics'
          message: 'Update metrics'
          author_name: 'github-actions'
          author_email: 'actions@github.com'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
